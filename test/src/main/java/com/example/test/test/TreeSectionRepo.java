package com.example.test.test;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;

import java.util.List;


// public interface TreeSectionRepo extends JpaRepository<TreeSection, String>{
  
//   List<TreeSection> findBySectionParentCd(String sectionParentCd);

//   @Query("SELECT ts FROM tree_section ts WHERE ts.sectionParentCd IN :sectionCds")
//   List<TreeSection> findAllIncludesSectionCds(List<String> sectionCds);
// }
